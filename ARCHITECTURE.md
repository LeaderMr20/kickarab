# 🏗️ شرح هيكل المشروع - KickArab

## 📦 الملفات الرئيسية

### `/context/AuthContext.js`
**الغرض:** إدارة حالة المصادقة والمستخدمين

**الميزات:**
- إدارة دورة حياة المستخدم (تسجيل، دخول، خروج)
- تخزين بيانات المستخدمين في localStorage
- دعم طرق المصادقة المتعددة (Google, Email)
- حماية كلمات المرور بـ Hash

**المفاهيم الأساسية:**
```javascript
- useAuth()                // Hook للوصول للبيانات
- AuthProvider            // Component يوفر البيانات لكل التطبيق
- user                    // بيانات المستخدم الحالي
- loading                 // حالة التحميل
- register()              // إنشاء حساب جديد
- login()                 // تسجيل الدخول برقم وكلمة
- loginWithGoogle()       // تسجيل الدخول عبر جوجل
- logout()                // تسجيل الخروج
- updateProfile()         // تحديث ملف المستخدم
```

---

### `/components/AuthModal.js`
**الغرض:** نموذج تسجيل الدخول والتسجيل

**الميزات:**
- واجهة جميلة وسهلة الاستخدام
- دعم Google Sign-In
- تسجيل بخطوتين
- معالجة الأخطاء والتحقق من البيانات
- الرسائل باللغة العربية

**الخطوات:**
1. المستخدم يختار "حساب جديد" أو "تسجيل دخول"
2. إذا حساب جديد: يملأ البيانات الأساسية ثم الملف الشخصي
3. إذا تسجيل دخول: يدخل البريد وكلمة المرور
4. يمكن اختيار Google Sign-In من أي مكان

---

### `/components/ProfileModal.js`
**الغرض:** عرض وتعديل ملف المستخدم الشخصي

**الميزات:**
- عرض بيانات المستخدم الحالية
- تعديل الملف الشخصي
- زر تسجيل الخروج
- واجهة ودية وسهلة الاستخدام

**البيانات المحفوظة:**
- الاسم الكامل
- الفريق المفضل
- المركز المفضل
- المدينة
- السيرة الذاتية (Bio)

---

### `/pages/discussions.js`
**الغرض:** صفحة النقاشات والحوار

**الميزات:**
- عرض قائمة النقاشات
- إمكانية الفلترة حسب الفئات
- إنشاء نقاش جديد
- عرض تفاصيل النقاش الكامل
- الرد على النقاشات
- نظام الإعجابات

**الهيكل:**
```
الصفحة الرئيسية
├── Sidebar (بار جانبي)
│   ├── الفئات
│   ├── ملف المستخدم
│   └── الإحصائيات
├── القائمة الرئيسية
│   ├── زر إنشاء نقاش
│   ├── نموذج النقاش الجديد
│   └── قائمة النقاشات
└── عرض التفاصيل (عند اختيار نقاش)
    ├── تفاصيل النقاش
    ├── الردود
    └── نموذج الرد الجديد
```

---

### `/pages/api/discussions.js`
**الغرض:** API endpoint للنقاشات

**الطلبات المدعومة:**
- `GET /api/discussions` - الحصول على جميع النقاشات
- `POST /api/discussions` - إنشاء نقاش جديد

**تنسيق البيانات:**
```javascript
{
  title: string,      // عنوان النقاش
  content: string,    // محتوى النقاش
  category: string,   // فئة النقاش
  author: string,     // اسم المؤلف
  userId: string      // معرف المستخدم
}
```

---

### `/components/Header.js`
**الغرض:** شريط التنقل العلوي

**الميزات:**
- شعار المشروع
- روابط التنقل الرئيسية
- عرض بيانات المستخدم
- زر ملف المستخدم الشخصي
- قائمة جوال (Mobile Menu)

**الروابط:**
- الرئيسية (/)
- المباريات (#matches)
- نجوم الأسبوع (#weekly-stars)
- الأخبار (#news)
- أخبار الهلال (#team-news)
- المنتديات (/discussions)
- عن KickArab (#about)

---

## 📊 نموذج البيانات

### المستخدم (User)
```javascript
{
  id: "123456",                    // معرف فريد
  name: "أحمد علي",               // الاسم الكامل
  username: "ahmed_ali",           // اسم المستخدم
  email: "ahmed@example.com",      // البريد الإلكتروني
  password: "hashed_password",     // كلمة المرور المشفرة
  avatar: "url_to_avatar",         // صورة الملف الشخصي
  bio: "محب كرة القدم",           // السيرة الذاتية
  favoriteTeam: "الهلال",          // الفريق المفضل
  position: "مهاجم",               // المركز المفضل
  city: "الرياض",                 // المدينة
  authMethod: "email",             // طريقة المصادقة (email/google)
  createdAt: "2026-02-13T..."      // تاريخ الإنشاء
}
```

### النقاش (Discussion)
```javascript
{
  id: 1,                           // معرف فريد
  title: "ما أفضل تشكيلة للهلال؟", // عنوان النقاش
  content: "شارك رأيك...",         // محتوى النقاش
  category: "tactics",             // الفئة
  author: "أحمد علي",             // اسم المؤلف
  userId: "123456",                // معرف المستخدم
  replies: [                       // الردود
    {
      id: 1,
      author: "محمد",
      content: "أنا أوافق",
      likes: 5,
      timestamp: "قبل ساعة",
      userId: "789"
    }
  ],
  likes: 15,                       // عدد الإعجابات
  views: 142,                      // عدد المشاهدات
  timestamp: "قبل يومين"           // الوقت
}
```

---

## 🔄 تدفق البيانات

### تسجيل مستخدم جديد
```
المستخدم يملأ النموذج
        ↓
AuthModal يتحقق من البيانات
        ↓
AuthContext.register() يحفظ المستخدم
        ↓
البيانات تُحفظ في localStorage
        ↓
يتم تعيين المستخدم في الحالة
        ↓
تحديث الواجهة لعرض بيانات المستخدم
```

### تسجيل دخول عبر جوجل
```
المستخدم يضغط "Google Sign-In"
        ↓
handleGoogleSignIn() يحاكي عملية Google
        ↓
AuthContext.loginWithGoogle() يعالج البيانات
        ↓
إنشاء/تحديث حساب المستخدم
        ↓
حفظ في localStorage
        ↓
إغلاق النموذج وعرض ملف المستخدم
```

### إنشاء نقاش جديد
```
المستخدم يملأ نموذج النقاش
        ↓
التحقق من الصلاحيات (يجب تسجيل دخول)
        ↓
إنشاء كائن النقاش
        ↓
إضافة إلى قائمة النقاشات
        ↓
حفظ في localStorage
        ↓
تحديث الصفحة لعرض النقاش الجديد
```

---

## 🎨 نظام الألوان والموضوعات

### الألوان الرئيسية
```css
--brand-500: #FF6B35      /* البرتقالي الرئيسي */
--pitch: #0a0e27          /* لون الملعب */
--gray-700: #1f2937       /* رمادي داكن */
--white: #ffffff          /* أبيض */
```

### الفئات والألوان
```javascript
general:   💬 أزرق      (bg-blue-500/10)
tactics:   📋 بنفسجي    (bg-purple-500/10)
mercato:   🔄 أخضر      (bg-green-500/10)
leagues:   🏆 أصفر      (bg-yellow-500/10)
players:   ⭐ وردي      (bg-pink-500/10)
teams:     🏟️ برتقالي   (bg-orange-500/10)
```

---

## 🔐 أمان البيانات

### الطرق المستخدمة حالياً
1. **Hash بسيط** - لتشفير كلمات المرور (غير آمن للإنتاج)
2. **localStorage** - لحفظ البيانات محلياً
3. **Validation** - التحقق من صحة البيانات على الجانب العميل

### التحسينات المطلوبة للإنتاج
1. **bcryptjs** - لتشفير آمن لكلمات المرور
2. **قاعدة بيانات** - لتخزين آمن للبيانات
3. **JWT Tokens** - لإدارة الجلسات
4. **HTTPS** - لتشفير الاتصالات
5. **Server-side Validation** - التحقق من الجانب الخادم

---

## 📱 الاستجابية (Responsive Design)

### نقاط الكسر
- **Mobile:** < 640px
- **Tablet:** 640px - 1024px
- **Desktop:** > 1024px

### المكونات المستجيبة
- Header مع Mobile Menu
- AuthModal يعمل على جميع الأحجام
- ProfileModal مرن في التخطيط
- صفحة Discussions مع Sidebar قابل للإخفاء

---

## 🚀 الأداء والتحسينات

### التحسينات المطبقة
- ✅ Lazy Loading للصور
- ✅ Code Splitting
- ✅ Local Storage Caching
- ✅ Optimistic Updates

### التحسينات المخطط لها
- [ ] Database Caching
- [ ] Service Workers
- [ ] Image Optimization
- [ ] Code Bundling

---

## 🧪 الاختبار

### اختبارات متاحة
- الاختبارات الأساسية في `TESTS.js`
- أمثلة الاستخدام في `USAGE_EXAMPLES.js`

### كيفية التشغيل
```bash
# فتح الـ Developer Console
F12 أو Ctrl+Shift+J

# نسخ محتوى TESTS.js والصق في الكونسول
# سيتم تشغيل جميع الاختبارات تلقائياً
```

---

## 🔗 الملفات المرتبطة

- `package.json` - المكتبات والإعدادات
- `next.config.js` - إعدادات Next.js
- `tailwind.config.js` - إعدادات Tailwind
- `postcss.config.js` - إعدادات PostCSS

---

## 📚 المراجع والموارد

- [Next.js Documentation](https://nextjs.org/docs)
- [React Hooks](https://react.dev/reference/react)
- [Tailwind CSS](https://tailwindcss.com)
- [Firebase Auth](https://firebase.google.com/docs/auth)

---

**آخر تحديث:** فبراير 2026
**الإصدار:** 1.0.0
