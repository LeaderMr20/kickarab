# KickArab - المنصة العربية الأولى لكرة القدم

## المميزات المضافة

### 1. **نظام المصادقة المتقدم**

#### تسجيل الدخول عبر جوجل
- تسجيل دخول فوري عبر حساب جوجل
- ربط حسابات متعددة بنفس البريد الإلكتروني
- تخزين البيانات محلياً في localStorage

#### تسجيل الدخول برقم المستخدم وكلمة المرور
- التحقق الآمن من بيانات المستخدم
- حماية كلمات المرور باستخدام Hash بسيط (استخدم bcrypt في الإنتاج)
- التحقق من عدم تكرار أسماء المستخدمين والبريد الإلكتروني

#### التسجيل الجديد (حسابات جديدة)
- عملية تسجيل خطوتين:
  - الخطوة 1: إدخال البيانات الأساسية (الاسم، اسم المستخدم، البريد، كلمة المرور)
  - الخطوة 2: إكمال الملف الشخصي (الفريق المفضل، المركز، المدينة)

### 2. **نظام المنتديات والنقاشات**

#### الميزات الرئيسية:
- **إنشاء نقاشات جديدة** مع اختيار الفئة
- **6 فئات نقاش**:
  - 💬 نقاش عام
  - 📋 التكتيك والفن
  - 🔄 السوق والانتقالات
  - 🏆 الدوريات
  - ⭐ اللاعبون
  - 🏟️ الأندية

#### التفاعل والحوار:
- الرد على النقاشات
- نظام الإعجابات (Likes)
- عداد المشاهدات
- عرض عدد الردود

#### واجهة المستخدم:
- عرض قائمة النقاشات
- اختيار النقاش لعرض التفاصيل الكاملة
- نظام التصفية حسب الفئات
- معلومات المؤلف والوقت

### 3. **ملف المستخدم الشخصي (Profile)**

#### المعلومات المحفوظة:
- الاسم الكامل
- اسم المستخدم
- البريد الإلكتروني
- الفريق المفضل
- المركز المفضل
- المدينة
- السيرة الذاتية

#### الميزات:
- عرض ملف المستخدم من أي مكان
- تعديل البيانات الشخصية
- تسجيل الخروج من ملف المستخدم
- التحقق من طريقة المصادقة (جوجل / بريد إلكتروني)

### 4. **التحسينات على Header**

- إضافة رابط "المنتديات" للملاحة
- عرض بطاقة المستخدم عند تسجيل الدخول
- زر لفتح ملف المستخدم الشخصي
- تسجيل الخروج من ملف المستخدم

## الملفات المضافة والمعدلة

### الملفات الجديدة:
```
pages/discussions.js           - صفحة المنتديات والنقاشات
pages/api/discussions.js       - API للنقاشات
components/ProfileModal.js    - مكون ملف المستخدم الشخصي
```

### الملفات المعدلة:
```
package.json                   - إضافة المكتبات المطلوبة
context/AuthContext.js         - تطوير نظام المصادقة المتقدم
components/AuthModal.js        - إضافة Google Sign-In والتحسينات
components/Header.js           - إضافة ProfileModal والتحديثات
```

## التثبيت والتشغيل

### المتطلبات:
- Node.js 16+
- npm أو yarn

### الخطوات:

1. **تثبيت المكتبات:**
```bash
npm install
```

2. **تشغيل الخادم:**
```bash
npm run dev
```

3. **فتح المتصفح:**
```
http://localhost:3000
```

## كيفية الاستخدام

### تسجيل حساب جديد:
1. اضغط على زر "انضم الآن" في الرأس أو الصفحة الرئيسية
2. اختر "حساب جديد"
3. أدخل بياناتك الأساسية
4. أكمل ملفك الشخصي
5. تم! حسابك جاهز

### تسجيل الدخول عبر جوجل:
1. اضغط على زر "انضم الآن"
2. اضغط على زر "تسجيل الدخول عبر جوجل"
3. تم دخولك!

### المشاركة في المنتديات:
1. تأكد من تسجيل دخولك
2. اذهب لصفحة "المنتديات"
3. اضغط "ابدأ نقاش جديد"
4. اختر الفئة والعنوان والمحتوى
5. شارك رأيك مع المجتمع!

## هيكل البيانات

### نموذج المستخدم (User):
```javascript
{
  id: string,
  name: string,
  username: string,
  email: string,
  password: string (مشفرة - لا تُحفظ في الواجهة),
  avatar: string (URL),
  bio: string,
  favoriteTeam: string,
  position: string,
  city: string,
  authMethod: "email" | "google",
  createdAt: ISO8601 timestamp
}
```

### نموذج النقاش (Discussion):
```javascript
{
  id: number,
  title: string,
  content: string,
  category: string,
  author: string,
  userId: string,
  replies: Array<Reply>,
  likes: number,
  views: number,
  timestamp: string
}
```

### نموذج الرد (Reply):
```javascript
{
  id: number,
  author: string,
  content: string,
  likes: number,
  timestamp: string,
  userId: string
}
```

## ملاحظات أمان مهمة

### للإنتاج (Production):
1. **استخدام bcrypt** لتشفير كلمات المرور بشكل آمن
   ```bash
   npm install bcryptjs
   ```

2. **استخدام قاعدة بيانات حقيقية** (MongoDB, PostgreSQL, إلخ)

3. **تفعيل Google OAuth** مع مفاتيح API حقيقية

4. **استخدام JWT** لإدارة الجلسات

5. **HTTPS** لجميع الاتصالات

6. **التحقق من الجانب الخادم** لجميع البيانات

7. **معدل تحديد** (Rate Limiting) للـ API

## الخطوات التالية (Future Enhancements)

- [ ] تفعيل Google OAuth الحقيقي
- [ ] نقل البيانات إلى قاعدة بيانات حقيقية
- [ ] نظام التنبيهات والإشعارات
- [ ] البحث والفرز المتقدم
- [ ] نظام التصنيفات (Ratings/Reviews)
- [ ] نظام المشاركات والنقاط
- [ ] التكامل مع وسائل التواصل الاجتماعي
- [ ] نسخة موبايل

## المشاكل المعروفة وحلولها

### المشكلة: فقدان البيانات عند تحديث الصفحة
**الحل:** تثبيت قاعدة بيانات حقيقية

### المشكلة: عدم عمل Google OAuth
**الحل:** الحالية محاكاة فقط. يتطلب تفعيل OAuth الحقيقي

## الدعم والمساعدة

للمزيد من المعلومات أو الإبلاغ عن مشاكل، يرجى التواصل معنا.

---

**تم الإنشاء بواسطة:** GitHub Copilot
**التاريخ:** فبراير 2026
**الإصدار:** 1.0.0
