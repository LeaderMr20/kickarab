# 🧪 دليل الاختبار الشامل - KickArab

## 📝 خطة الاختبار

هذا الدليل يوضح كيفية اختبار جميع ميزات KickArab بنجاح.

---

## 1️⃣ اختبار نظام المصادقة

### أ. اختبار التسجيل الجديد

**الخطوات:**
```
1. فتح http://localhost:3000
2. اضغط "ابدأ الآن" أو "انضم الآن"
3. اختر "حساب جديد"
4. ملأ النموذج:
   - الاسم: أحمد علي
   - اسم المستخدم: ahmed_ali_2026
   - البريد: ahmed@example.com
   - كلمة المرور: Secure123!
   - تأكيد كلمة المرور: Secure123!
5. اضغط "التالي"
6. اختر فريقك المفضل (مثلاً: الهلال)
7. اختر المركز (اختياري)
8. أدخل المدينة (اختياري)
9. اضغط "أنشئ حسابي"
```

**المتوقع:**
- ✅ ظهور رسالة نجاح
- ✅ إغلاق النموذج
- ✅ عرض بيانات المستخدم في الـ Header
- ✅ حفظ البيانات في localStorage

**الاختبارات الإضافية:**
```
❌ تجربة بريد موجود: يجب أن يظهر خطأ
❌ تجربة كلمة مرور قصيرة: يجب أن يظهر خطأ
❌ عدم تطابق كلمات المرور: يجب أن يظهر خطأ
❌ ترك حقل فارغ: يجب أن يظهر خطأ
```

### ب. اختبار تسجيل الدخول برقم وكلمة

**الخطوات:**
```
1. فتح http://localhost:3000
2. اضغط "انضم الآن"
3. اختر "تسجيل دخول"
4. أدخل البريد: ahmed@example.com
5. أدخل كلمة المرور: Secure123!
6. اضغط "تسجيل الدخول"
```

**المتوقع:**
- ✅ ظهور رسالة نجاح
- ✅ إغلاق النموذج
- ✅ عرض بيانات المستخدم

**الاختبارات الإضافية:**
```
❌ بريد خاطئ: خطأ
❌ كلمة مرور خاطئة: خطأ
❌ حقول فارغة: خطأ
```

### ج. اختبار Google Sign-In

**الخطوات:**
```
1. فتح http://localhost:3000
2. اضغط "انضم الآن"
3. اضغط زر "تسجيل الدخول عبر جوجل"
4. انتظر ثانيتين (محاكاة)
5. تم الدخول!
```

**المتوقع:**
- ✅ رسالة انتظار
- ✅ دخول تلقائي
- ✅ إنشاء حساب جديد أو دخول موجود

### د. اختبار الملف الشخصي

**الخطوات:**
```
1. تأكد من تسجيل دخولك
2. اضغط على بطاقة المستخدم في الـ Header
3. يفتح ProfileModal
4. اضغط "تعديل الملف"
5. عدّل البيانات:
   - غير الاسم
   - غير الفريق المفضل
   - أضف مدينة
   - أضف bio
6. اضغط "حفظ التغييرات"
```

**المتوقع:**
- ✅ حفظ البيانات الجديدة
- ✅ تحديث عرض البيانات
- ✅ حفظ في localStorage

### هـ. اختبار تسجيل الخروج

**الخطوات:**
```
1. اضغط على المستخدم في الـ Header
2. اضغط "خروج" أو افتح ProfileModal
3. اضغط "خروج"
```

**المتوقع:**
- ✅ حذف بيانات المستخدم
- ✅ إظهار زر "انضم الآن" بدل بيانات المستخدم
- ✅ مسح localStorage

---

## 2️⃣ اختبار المنتديات والنقاشات

### أ. الوصول لصفحة المنتديات

**الخطوات:**
```
1. فتح http://localhost:3000
2. اضغط على "المنتديات" في الـ Header
3. أو فتح مباشرة: http://localhost:3000/discussions
```

**المتوقع:**
- ✅ تحميل الصفحة
- ✅ عرض القائمة الجانبية بالفئات
- ✅ عرض قائمة النقاشات

### ب. اختبار إنشاء نقاش جديد (بدون تسجيل دخول)

**الخطوات:**
```
1. اذهب لصفحة المنتديات بدون تسجيل دخول
2. اضغط "ابدأ نقاش جديد"
```

**المتوقع:**
- ✅ ظهور رسالة: "يجب عليك تسجيل الدخول أولاً"
- ⚠️ عدم فتح نموذج النقاش

### ج. اختبار إنشاء نقاش جديد (مع تسجيل دخول)

**الخطوات:**
```
1. تسجيل دخول أولاً
2. اذهب للمنتديات
3. اضغط "ابدأ نقاش جديد"
4. ملأ النموذج:
   - الفئة: التكتيك والفن
   - العنوان: ما أفضل تشكيلة للهلال؟
   - المحتوى: شارك آراءك حول تشكيلة الهلال...
5. اضغط "نشر النقاش"
```

**المتوقع:**
- ✅ ظهور النقاش الجديد في القائمة
- ✅ تحديث البيانات تلقائياً
- ✅ حفظ في localStorage
- ✅ إغلاق النموذج

### د. اختبار فلترة النقاشات

**الخطوات:**
```
1. اضغط على فئة مختلفة في الـ Sidebar
   (مثلاً: "السوق والانتقالات")
2. يجب أن تظهر فقط النقاشات من هذه الفئة
3. جرب جميع الفئات
```

**المتوقع:**
- ✅ تحديث القائمة عند اختيار فئة
- ✅ عرض البيانات الصحيحة
- ✅ رسالة "لا توجد نقاشات" إذا كانت الفئة فارغة

### هـ. اختبار عرض تفاصيل النقاش

**الخطوات:**
```
1. اضغط على نقاش من القائمة
2. يفتح تفاصيل كامل النقاش
3. عرض:
   - العنوان والمحتوى
   - اسم المؤلف والوقت
   - عدد المشاهدات والإعجابات
   - الردود الحالية
```

**المتوقع:**
- ✅ عرض كامل البيانات
- ✅ واجهة واضحة
- ✅ نموذج الرد في الأسفل

### و. اختبار الرد على النقاشات

**الخطوات:**
```
1. فتح نقاش (كنت مسجل دخول)
2. اكتب رد في نموذج "أضف ردك"
3. اضغط "إرسال الرد"
```

**المتوقع:**
- ✅ إضافة الرد للقائمة
- ✅ عرض اسم المؤلف والوقت
- ✅ مسح نموذج الرد
- ✅ تحديث عدد الردود

### ز. اختبار الإعجابات

**الخطوات:**
```
1. في صفحة النقاش، اضغط 👍
2. شاهد تزايد الرقم
3. قد تضغط عدة مرات
```

**المتوقع:**
- ✅ تزايد عدد الإعجابات
- ✅ تحديث في الواجهة مباشرة

### ح. اختبار العودة للقائمة

**الخطوات:**
```
1. في صفحة تفاصيل النقاش
2. اضغط "← العودة للنقاشات"
3. عودة لقائمة النقاشات
```

**المتوقع:**
- ✅ عودة سلسة وبدون أخطاء

---

## 3️⃣ اختبار الواجهة والتجربة

### أ. اختبار Dark Mode

**الخطوات:**
```
1. تصفح الموقع
2. لاحظ الألوان:
   - الخلفية: أسود/رمادي داكن
   - الكائن: أبيض/رمادي فاتح
   - الأزرار: برتقالي (#FF6B35)
```

**المتوقع:**
- ✅ تصميم Dark Mode متقن
- ✅ لا توجد أجزاء بيضاء قاسية
- ✅ تباين جيد

### ب. اختبار Responsive Design

**الخطوات:**
```
على Desktop:
1. عرض كامل بـ 1920px أو أكثر
2. شريط جانبي مرئي
3. قائمة التنقل كاملة

على Tablet (768px):
1. تقليل الشريط الجانبي
2. ظهور Mobile Menu

على Mobile (375px):
1. قائمة جوال (Hamburger Menu)
2. تخطيط عمودي
3. زر الـ Sidebar قابل للإخفاء
```

**للاختبار:**
- فتح Developer Tools (F12)
- اختبار على أحجام مختلفة
- التحقق من سهولة الاستخدام

### ج. اختبار رسائل الخطأ

**الخطوات:**
```
1. حاول تسجيل دخول ببريد خاطئ
2. حاول ترك حقول فارغة
3. حاول كلمة مرور قصيرة
4. لاحظ الرسائل الواضحة باللون الأحمر
```

**المتوقع:**
- ✅ رسائل عربية واضحة
- ✅ لون أحمر للأخطاء
- ✅ يختفي الخطأ عند التعديل

---

## 4️⃣ اختبار التخزين والبيانات

### أ. التحقق من localStorage

**في المتصفح:**
```
1. افتح Developer Tools (F12)
2. اذهب للـ Application Tab
3. اختر localStorage
4. ابحث عن:
   - kickarab_user (بيانات المستخدم)
   - kickarab_users_db (قاعدة المستخدمين)
   - kickarab_discussions (النقاشات)
```

**المتوقع:**
- ✅ بيانات صحيحة JSON
- ✅ تحديث عند كل تغيير
- ✅ حذف عند تسجيل الخروج

### ب. اختبار حفظ البيانات

**الخطوات:**
```
1. سجل دخول
2. أنشئ نقاش
3. اضغط F5 (تحديث الصفحة)
4. البيانات موجودة؟
```

**المتوقع:**
- ✅ المستخدم لا يزال مسجل دخول
- ✅ النقاش موجود في القائمة
- ✅ البيانات لم تُفقد

### ج. اختبار مسح البيانات

**الخطوات:**
```
1. Developer Tools → Application
2. localStorage
3. حذف kickarab_user
4. تحديث الصفحة (F5)
5. يجب أن يكون تسجيل الدخول قد حُذف
```

**المتوقع:**
- ✅ حذف فوري
- ✅ تحديث الواجهة تلقائياً

---

## 5️⃣ اختبارات الأمان

### أ. اختبار التحقق من البيانات

**الخطوات:**
```
اختبر جميع الحالات:
❌ بريد خاطئ الصيغة
❌ كلمة مرور قصيرة جداً
❌ أسماء مستخدمين مكررة
❌ بريد مكرر
✅ جميع الحالات تعطي رسائل خطأ واضحة
```

### ب. اختبار حماية كلمات المرور

**الخطوات:**
```
1. افتح Developer Tools
2. في localStorage ابحث عن كلمة المرور
3. لا يجب أن تظهر الكلمة الأصلية
4. يجب أن تكون مشفرة (hash)
```

---

## 6️⃣ اختبار الأداء

### أ. سرعة التحميل

**الخطوات:**
```
1. فتح http://localhost:3000
2. قياس وقت التحميل (من DevTools)
3. يجب أن يكون < 3 ثواني
```

### ب. استجابة الواجهة

**الخطوات:**
```
1. اضغط الأزرار
2. جرب الانتقال بين الصفحات
3. كل شيء يجب أن يكون سلس بدون تأخير
```

---

## 7️⃣ اختبارات الحالات الحدية (Edge Cases)

### أ. ماذا إذا لم يكن هناك نقاشات؟
```
❌ فلترة إلى فئة فارغة
✅ يجب أن تظهر رسالة "لا توجد نقاشات"
```

### ب. ماذا إذا كان النقاش بدون ردود؟
```
❌ فتح نقاش جديد (بدون ردود)
✅ عرض "لا توجد ردود حتى الآن"
```

### ج. نصوص طويلة جداً
```
❌ كتابة نقاش جداً طويل (500+ حرف)
✅ يجب أن يعمل بدون مشاكل
```

---

## ✅ قائمة التحقق النهائية

### قبل الإطلاق للعموم:
- [ ] جميع الاختبارات تمر بنجاح
- [ ] لا توجد رسائل خطأ في Console
- [ ] تحميل الموقع سريع (< 3s)
- [ ] يعمل على جميع المتصفحات
- [ ] يعمل على الموبايل
- [ ] البيانات تُحفظ بشكل صحيح
- [ ] لا توجد مشاكل أمان واضحة

---

## 🐛 الإبلاغ عن الأخطاء

إذا وجدت خطأ:
1. وصّف المشكلة
2. اذكر خطوات التكرار
3. اقطع صورة/فيديو إن أمكن
4. أرسل البلاغ: support@kickarab.com

---

<div align="center">

**استمتع بالاختبار! 🚀**

أي مشاكل؟ اقرأ DOCUMENTATION.md للمزيد من المعلومات.

</div>
